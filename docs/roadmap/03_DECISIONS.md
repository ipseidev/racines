# Registre des décisions techniques

Format : décision, pourquoi, alternatives écartées, réversibilité. Numérotation `T-nn`. Les décisions produit (D-7, D-8, D-9) restent dans le dossier produit, doc 05 R-12.

## Décisions fondatrices (2 septembre 2026)

**T-01 — Stack : Laravel + Inertia (React, TypeScript) + Postgres.** Pourquoi : maîtrise de l'équipe, écosystème Spatie/Filament mature, Inertia évite une API séparée tout en gardant React pour les interfaces riches (recorder, lecteur). Écartées : Next.js (deuxième langage, hébergement UE moins simple), Livewire pour le front utilisateur (moins adapté au recorder). Réversibilité : faible, c'est le socle.

**T-02 — Hébergement : serveur DigitalOcean existant piloté par Forge, région européenne obligatoire.** Pourquoi : déjà en place, Forge gère PHP, Redis, Horizon, scheduler, SSL. Condition : le droplet doit être en `ams3` ou `fra1` ; si ce n'est pas le cas, créer un droplet UE et y rattacher le site avant le bloc 16. Écartées : Laravel Cloud (AWS, moins de contrôle sur la région et le coût), Scaleway (pas d'outillage existant). Réversibilité : moyenne, Forge est portable vers un autre VPS.

**T-03 — Base de données : Postgres managé DigitalOcean dans la même région, sauvegardes quotidiennes et PITR activés.** Pourquoi : le dossier exige une restauration testée et un RPO borné ; le managé donne le PITR sans opération. Écartée : Postgres installé par Forge sur le droplet (moins cher, mais restauration artisanale). Réversibilité : élevée.

**T-04 — Stockage objet : Cloudflare R2, buckets créés avec juridiction UE, endpoint `*.eu.r2.cloudflarestorage.com`.** Pourquoi : choix de l'équipe, S3-compatible, pas de frais de sortie. Point de vigilance : Cloudflare est une société américaine ; la restriction de juridiction, le DPA et une mention dans l'AIPD sont obligatoires (bloc 16). Alternative sans changement de code : Scaleway Object Storage (même driver S3). Trois buckets : `media`, `media-replica`, `backups`, chacun avec un jeton d'accès distinct. Réversibilité : élevée.

**T-05 — Admin interne : Filament, réservé au back-office. Interfaces utilisateur : React custom.** Pourquoi : Filament accélère le support ; jugé trop brut pour des seniors et des acheteurs. Réversibilité : élevée.

**T-06 — Email : Resend (transport Laravel officiel `resend/resend-laravel`). SMS : Twilio via le SDK officiel `twilio/sdk` derrière l'interface `SmsSender` (pas de package de canal tiers, pour garder un seul point d'abstraction).** Pourquoi : choix de l'équipe. Twilio supporte l'expéditeur alphanumérique en France pour les messages sortants sans réponse ; un numéro de repli reste configuré. Réversibilité : élevée, interfaces `SmsSender` et mailer Laravel.

**T-07 — Rendu Fluide : Claude via le SDK PHP officiel `anthropic-ai/sdk`, modèle `claude-opus-5`, `outputConfig.effort = medium`, sortie structurée par schéma JSON, prompt système mis en cache.** Pourquoi : qualité de réécriture en français, SDK officiel, sortie structurée qui évite tout parsing fragile. Le modèle et l'effort sont des variables d'environnement. Le repli serveur en cas de refus (`fallbacks`) est activé par défaut sur l'appel bêta ; si le SDK installé ne l'expose pas, on gère `stopReason = refusal` en conservant le verbatim seul. Écartée : Mistral (souveraineté, mais qualité non évaluée par l'équipe). Réversibilité : élevée, interface `StoryRenderer`.

**T-08 — Transcription : Gladia comme premier adaptateur, Deepgram comme second, décision par banc d'essai WER sur corpus de voix âgées au bloc 06.** Pourquoi : Gladia est française, hébergée en UE, bonne en français, avec vocabulaire personnalisé et horodatage des mots ; Deepgram sert de référence de qualité. Voxtral (Mistral) non retenu faute de connaissance de l'équipe ; peut devenir un troisième adaptateur. Réversibilité : élevée, interface `TranscriptionProvider`.

**T-09 — Capture audio : `MediaRecorder` natif, tranches de 5 s stockées dans IndexedDB, upload multipart résumable vers R2 par URLs présignées (parts de 5 Mio).** Pourquoi : aucune dépendance lourde, reprise après rechargement, l'objet n'est confirmé qu'après `CompleteMultipartUpload` et `HeadObject`. Écartées : tus (serveur supplémentaire), upload via le serveur PHP (double transfert). Réversibilité : moyenne.

**T-10 — Traitement audio : ffmpeg via `pbmedia/laravel-ffmpeg` ; l'original n'est jamais réencodé, un MP3 128 kb/s est dérivé pour la lecture et l'export.** Réversibilité : élevée.

**T-11 — PDF du livre : `spatie/browsershot` (Chromium) avec Paged.js pour la mise en page, une passe Ghostscript vers PDF/X-1a quand l'imprimeur sera connu.** Pourquoi : HTML/CSS testable, un seul moteur de rendu, pas de service supplémentaire. Écartées : Prince (payant), LaTeX/Typst (courbe d'apprentissage), Gotenberg (Docker absent sur Forge). Réversibilité : moyenne.

**T-12 — QR : `endroid/qr-code`, URL `https://{LINKS_DOMAIN}/q/{token}`.** Réversibilité : élevée.

**T-13 — Machine d'états : `spatie/laravel-model-states` avec classes de transition explicites et gardes.** Pourquoi : transitions déclaratives, testables une à une, impossibilité structurelle d'une validation implicite. Réversibilité : moyenne.

**T-14 — Réglages : `spatie/laravel-settings` (`BrandSettings`, `PilotSettings`) édités dans Filament ; `config/brand.php` porte les défauts.** Pourquoi : le nom, le domaine et les couleurs doivent changer sans déploiement. Réversibilité : élevée.

**T-15 — Feature flags : Laravel Pennant, portée projet.** Pourquoi : les deux variantes de validation et les tests de prix du dossier sont des flags par cohorte. Réversibilité : élevée.

**T-16 — Paiement : Stripe Checkout (page hébergée) + Laravel Cashier pour clients, webhooks et remboursements.** Pourquoi : conformité PCI minimale, prise en charge SCA, remboursements outillés. Réversibilité : moyenne.

**T-17 — Authentification : Fortify pour les Initiateur·rice·s (email + mot de passe, vérification d'email, 2FA optionnelle) ; Filament avec MFA obligatoire pour le back-office ; jetons porteurs et OTP pour narrateurs et proches.** Réversibilité : moyenne.

**T-18 — Journal d'audit : table `audit_logs` maison, append-only par trigger Postgres, chaîne de hachage `previous_hash → hash`.** Pourquoi : « journalisation inviolable de toutes les actions support (lecture comprise) » du doc 04 §12 ; un package générique ne bloque pas les `UPDATE`. Réversibilité : faible par nature.

**T-19 — Analytics : PostHog cloud UE, capture serveur par défaut, front uniquement hors pages à jeton, URL nettoyées.** Écartées : Plausible (pas de funnels par utilisateur), Matomo auto-hébergé (opération). Réversibilité : élevée.

**T-20 — Erreurs : Flare. Disponibilité et page de statut : Oh Dear.** Pourquoi : hébergés en UE, intégration Laravel native. Réversibilité : élevée.

**T-21 — i18n : fichiers `lang/fr/*.php` partagés au front par prop Inertia, hook `useT()` maison, test d'existence des clés.** Pourquoi : zéro dépendance, français seul au MVP, mais aucune chaîne en dur pour permettre EN/ES en Phase 2. Réversibilité : élevée.

**T-22 — Tests : Pest (parallèle), Larastan niveau 8, Vitest + React Testing Library, Playwright (Chromium en CI, matrice d'appareils réels manuelle), `@axe-core/playwright`.** Réversibilité : faible, c'est le contrat de qualité.

**T-23 — Identifiants : UUID ordonnés (`HasUuids`) pour tout modèle exposé ; jetons de 32 octets aléatoires encodés base64url, stockés hachés SHA-256.** Réversibilité : faible.

**T-24 — Environnement local : Laravel Sail avec `pgsql`, `redis`, `mailpit`, `minio` (émule R2 en local), `clamav` à partir du bloc 12 ; image Sail publiée et étendue avec ffmpeg et Chromium.** Réversibilité : élevée.

**T-25 — Organisation du dépôt : la racine est le projet Laravel ; les documents produit vont dans `docs/dossier/`, les captures dans `docs/reference/` (ignorées par git).** Pourquoi : Forge déploie la racine ; les documents restent versionnés avec le code. Réversibilité : élevée.

**T-26 — Démo « essayez en 60 secondes » : enregistrement local seulement, lecture immédiate, aucun envoi au serveur.** Pourquoi : zéro donnée personnelle collectée sans achat, zéro coût de stockage, et le test d'ergonomie porte sur le micro et le bouton, pas sur l'upload. Réversibilité : élevée.

**T-27 — Limites d'enregistrement : alerte douce à 10 min, arrêt à 20 min, taille maximale 200 Mo, formats acceptés `audio/webm`, `audio/mp4`, `audio/ogg`, `audio/mpeg`, `audio/wav`.** Pourquoi : le dossier ne fixe pas de durée par histoire ; le fair use mensuel (60 min) borne le total. Paramètres dans `config/product.php`. Réversibilité : élevée.

**T-28 — Heure d'envoi par défaut : cadeaux à 09:00 Europe/Paris ; prompts au créneau choisi par le narrateur (matin 09:00, après-midi 14:00, soir 18:00) le jour choisi ; tick du moteur toutes les heures à la minute 07 ; dispatch des prompts toutes les 5 minutes.** Réversibilité : élevée.

## Décisions prises en cours de route

Format : `T-nn — date — bloc — décision — pourquoi — à revalider par l'humain : oui/non`.

**T-95 — 2026-09-03 — bloc 09 — La règle détecte et agit, le tick enregistre.** L'interface de l'annexe C faisait écrire `engine_events` par chaque règle (`fire(): EngineEvent`) et calculer la clé d'idempotence à chacune (`occurrenceKey()`). Les deux sont remontés d'un cran : `fire()` rend le contenu de `action_taken`, le tick insère la ligne **avant** l'action et dans la même transaction, et `Occurrence` calcule la clé. Motif : la clé unique en base est ce qui empêche deux ticks concurrents d'envoyer deux fois le même message, et cette garantie ne doit pas dépendre de onze implémentations qui pourraient se tromper sur l'ordre. Corollaire : la formule de clé n'existe qu'à un endroit, donc elle ne peut pas diverger. À revalider : non.

**T-96 — 2026-09-03 — bloc 09 — Le test du vocabulaire interdit lit les valeurs traduites, pas les fichiers.** Il parcourait le contenu brut de `lang/fr` : un commentaire expliquant « ne jamais écrire *dernier rappel* » le faisait échouer, ce qui pousse à écrire des commentaires évasifs sur le sujet précis où il faut être explicite. Il charge désormais les tableaux et parcourt les chaînes. Deux effets de bord utiles : il ne peut plus être trompé par une expression coupée en deux, et il ne s'applique plus qu'aux fichiers **du produit** — « ce champ est obligatoire » dans `validation.php` est du français correct pour un formulaire, pas une faute de ton envers un narrateur. À revalider : non.

**T-97 — 2026-09-03 — bloc 09 — Un test comparait `now()->subHour()` au début de la journée.** `RecordConsentTest` créait une version 2.0 « il y a une heure » pour qu'elle prime sur la 1.0 semée « au début du jour ». Entre minuit et une heure, l'heure en arrière tombe la veille, et la 1.0 gagnait. Trouvé en travaillant après minuit, ce qui est la seule façon de trouver ce genre de chose. À revalider : non.

**T-98 — 2026-09-03 — bloc 09 — L'option téléphone ouvre un ticket, la ligne `phone_options` attend le bloc 10.** `OfferPhoneOption` vérifie le drapeau, refuse franchement quand il est fermé — « nous vous rappelons » qu'on ne tiendrait pas serait pire que rien — et ouvre le ticket support que le bloc demande. La ligne `phone_options(entry=rescue)` viendra avec sa table au bloc 10 ; le drapeau étant fermé par défaut, aucune famille n'est concernée entre-temps. À revalider : non, le bloc 10 la crée.

**T-99 — 2026-09-03 — bloc 09 — Un événement supprimé ne consomme pas l'idempotence, et ne compte pas comme un message.** Trouvé en écrivant la cinquième règle. Quand deux règles veulent parler au même narrateur le même jour, la seconde est consignée comme supprimée — mais elle écrivait la `dedupe_key` de l'occurrence, si bien que le rappel seulement **différé** n'aurait jamais pu partir ; et elle était comptée dans « deux rappels maximum », si bien que le narrateur en aurait reçu un au lieu de deux. Les événements supprimés portent désormais une clé datée, et tous les compteurs de limite lisent les messages **partis**. À revalider : non.

**T-100 — 2026-09-03 — bloc 09 — `TokenService::peek()` : vérifier un lien sans le consommer.** Une action en un tap doit **montrer** avant d'agir — un lien reçu par SMS qui agirait à l'ouverture serait déclenché par le simple aperçu d'un client de messagerie. Or la page de confirmation d'un jeton à usage unique le grillait avant que son propre bouton puisse jouer. `resolve()` devient `peek()` + consommation, et le middleware accepte `resolve.token:action,peek`. À revalider : non.

**T-101 — 2026-09-03 — bloc 09 — Le numéro de relance est le discriminant d'occurrence, jamais une date ni un compteur d'état.** Première version : la clé de « trois histoires sans réaction » portait le nombre d'histoires partagées, celle de l'alerte J+21 le mois courant. Dans les deux cas la clé ne changeait pas d'une fois sur l'autre, et la seconde sollicitation n'était jamais envoyée — la règle se croyait déjà passée. Les quatre règles concernées comptent maintenant leurs envois et utilisent ce numéro. À revalider : non.

**T-102 — 2026-09-03 — bloc 09 — « Aucune histoire depuis dix jours » inclut le dixième jour.** La détection excluait les projets ayant une histoire `recorded_at >= now-10j`, donc au dixième jour pile la règle se taisait et ne parlait qu'au onzième. Comparaison stricte : à dix jours pile, le silence dure bien dix jours. Vaut aussi pour la règle des vingt-et-un jours. À revalider : non.

**T-103 — 2026-09-03 — bloc 09 — `whereColumn` entre une clé polymorphe et un uuid casse en Postgres.** `access_tokens.subject_id` est un `varchar` (il porte des sujets de plusieurs tables), `stories.id` un `uuid` : Postgres refuse de les comparer sans transtypage, et l'erreur n'apparaît qu'à l'exécution de la requête. Le transtypage est explicite (`stories.id::text`) et commenté là où il se trouve, parce que la prochaine règle qui joindra les jetons rencontrera le même mur. À revalider : non.

**T-94 — 2026-09-02 — bloc 08 — La suite de tests impose son propre environnement.** Les `<env>` de `phpunit.xml` ne s'appliquent que si la variable est **absente** de l'environnement : l'intégration continue exportait `QUEUE_CONNECTION=database` pour son serveur de bout en bout, et les tests PHP héritaient de ce pilote. Trois tests de purge, verts en local avec une file synchrone, échouaient en CI pour une raison invisible dans le code. `force="true"` est posé sur ce que la suite **possède** — file, cache, session, courriel, et les quatre fournisseurs simulés — et laissé sur ce que chaque environnement possède, les hôtes de connexion. Corollaire : les tests qui dépendaient d'une file synchrone jouent désormais le job explicitement, parce qu'un test ne doit pas dépendre d'un pilote. À revalider : non.

**T-86 — 2026-09-02 — bloc 08 — Un lien porte désormais son détenteur.** `access_tokens.issued_by` disait qui a émis le lien ; il manquait qui le détient. Un lien d'histoire (`listen_story`) a une histoire pour sujet : sans porteur il est anonyme, ce qui contredit la règle §9 du bloc — « un lien par personne, jamais un lien famille commun » — et rend la visibilité restreinte inapplicable, puisqu'on ne sait plus qui écoute. `issued_to_type` / `issued_to_id` sont ajoutés, nullables, et un lien d'histoire sans porteur est **refusé** à la résolution plutôt que traité comme anonyme. Le support (bloc 11) en aura besoin aussi. À revalider : non.

**T-87 — 2026-09-02 — bloc 08 — Le middleware de jeton accepte une liste de types.** `resolve.token:listen_project|listen_story` : l'espace famille a deux portes qui mènent à la même page. Le périmètre reste **déclaré sur la route**, jamais deviné dans un contrôleur — c'est ce qui le rend vérifiable d'un coup d'œil sur le fichier de routes, et un test parcourt la table pour s'en assurer. À revalider : non.

**T-88 — 2026-09-02 — bloc 08 — L'URL d'écoute est signée sur l'adresse vue par le navigateur.** `MediaStorage::temporaryUrl()` déléguait à `Storage::temporaryUrl()`, qui signe sur l'endpoint du **serveur** : en local le navigateur ne le résout pas, et l'audio ne joue rien (`ERR_NAME_NOT_RESOLVED`). Même correction que pour les envois présignés du bloc 04 (T-56) : on signe avec le client `public`. Le défaut serait passé inaperçu jusqu'en production le jour où l'endpoint public diffère du privé — c'est-à-dire exactement le jour où R2 est derrière un domaine à soi. À revalider : non.

**T-89 — 2026-09-02 — bloc 08 — Après une navigation Inertia, `back()` ne revient pas où l'on croit.** Un proche arrive sur une histoire par navigation client ; l'en-tête `Referer` du navigateur pointe encore la liste. `back()` le renvoyait donc à la liste après sa réaction, en lui faisant perdre sa place **et** la confirmation de son geste. Les contrôleurs de l'espace famille redirigent explicitement vers la route de l'histoire. Règle générale : `back()` n'est fiable que sur une page atteinte par un chargement complet. À revalider : non.

**T-90 — 2026-09-02 — bloc 08 — Une histoire hors de portée rend un 404 amical, sans dire pourquoi.** `abort(404)` affichait la page d'erreur du framework : technique, en anglais, et avec des styles en ligne sans nonce — trois manquements d'un coup à la convention §16. `StoryUnavailable` rend une page Inertia en langage simple avec un retour vers la liste. Elle ne dit **pas** le motif : ni « pas encore validée », ni « masquée », ni « réservée au livre ». Un proche qui apprendrait qu'une histoire existe mais lui est refusée en saurait déjà trop, et le narrateur n'a pas à justifier ses retraits auprès de sa famille. À revalider : non.

**T-91 — 2026-09-02 — bloc 08 — Le lecteur audio ne compte pas un saut de curseur comme de l'écoute.** Les secondes rapportées sont mesurées sur l'avance réelle de la lecture, et une avance supérieure à deux secondes entre deux `timeupdate` est traitée comme un déplacement, pas comme une écoute. Sans ça, un proche qui tire le curseur jusqu'au bout « écouterait » deux minutes en un geste, et le maillon central de la chaîne H2 mesurerait n'importe quoi. Le reste part aussi à la pause et au `pagehide` : un onglet fermé à la vingt-neuvième seconde ne doit pas perdre la mesure. À revalider : non.

**T-92 — 2026-09-02 — bloc 08 — Le plafond de notification se lit dans les messages **envoyés**, pas dans les réactions.** « Une notification par histoire et par jour » est vérifié sur `outbound_messages`, qui sait ce qui est **parti** — et non sur un compteur d'intentions, qui compterait aussi ce qui a échoué. Trois proches qui réagissent le même soir font donc un message qui les nomme tous les trois. À revalider : non.

**T-93 — 2026-09-02 — bloc 08 — La façade d'analytics existe avant PostHog.** L'interface `Analytics` et l'implémentation `LogAnalytics` arrivent au bloc 08, alors que PostHog n'est prévu qu'au bloc 15. Le motif n'est pas l'anticipation : les événements de la chaîne H2 doivent être émis **pendant** qu'on écrit le code qui les produit, sinon ils sont ajoutés après coup, aux mauvais endroits, et la mesure ne veut plus rien dire. `LogAnalytics` garde en mémoire ce qu'il a capté, ce qui rend éprouvable dès maintenant qu'un événement part au bon moment, une seule fois, et sans donnée personnelle. À revalider : non.

**T-77 — 2026-09-02 — bloc 07 — Un neuvième consentement : la délégation de validation.** Le dossier 04 §2 en nomme huit ; le bloc 07 §6.7 exige un `consent_id` **non nullable** sur `mandates`, ce qui suppose un motif de consentement que le dossier n'avait pas nommé — il ne décrivait pas encore la délégation. `ConsentKind::MandateDelegation` est ajouté, la contrainte `check` de `consents.kind` migrée, et un texte de consentement semé. Le test des énumérations attend désormais neuf valeurs, avec la raison écrite à côté. À revalider : oui, la formulation du texte passe par la relecture juridique du bloc 10.

**T-78 — 2026-09-02 — bloc 07 — Le code de l'espace narrateur est semé, pas exposé par une route.** Le bloc prévoyait « une route de test locale » révélant le code envoyé. Écarté : une route qui expose des codes à usage unique est exactement le genre d'affordance qui finit activée quelque part, et l'exposer même derrière `APP_ENV` déplace le risque sans le supprimer. `E2ELinksSeeder` sème à la place trois narrateurs avec des coordonnées connues et un défi en attente dont le code est connu — le même parti que les liens déterministes du bloc 04. Corollaire produit : la page d'entrée offre « j'ai déjà un code », parce qu'un test qui n'en redemande pas est aussi le parcours de quelqu'un qui a fermé son onglet et retrouve son SMS. À revalider : non.

**T-79 — 2026-09-02 — bloc 07 — La borne par IP des routes à jeton est réglable, et desserrée hors production.** Les deux bornes de `throttle:tokens` ne protègent pas la même chose : celle par **jeton** (20/min) protège du balayage de liens, celle par **IP** (60/min) protège l'infrastructure. La seconde punit le partage de connexion — une maison de retraite, une famille derrière un routeur — et coupait la suite bout en bout, qui concentre trente navigateurs sur une adresse. Elle passe donc en configuration (`THROTTLE_TOKENS_PER_IP`, défaut 60) et vaut dix fois plus hors production. La borne par jeton, elle, est identique partout : c'est elle qui protège réellement les liens, et la relâcher aurait été relâcher la sécurité pour faire passer des tests. À revalider : oui, la valeur de production se réévalue au bloc 16 avec les mesures du pilote.

**T-80 — 2026-09-02 — bloc 07 — L'immuabilité de l'audio source cède devant l'effacement demandé.** Deux principes se rencontraient : « l'audio source est sacré, jamais supprimé **hors état `deleted`** » et le droit du narrateur à faire effacer son récit. Le déclencheur du bloc 04 lisait le premier sans sa réserve : il refusait aussi la mise à `null`, et `PurgeDeletedStory` échouait en base — le droit à l'effacement restait une intention. La règle complète : `original_path` ne peut pas **changer** de valeur, et ne peut être vidé que si l'histoire est `deleted`. L'immuabilité protège contre l'écrasement, pas contre l'effacement consenti. À revalider : non.

**T-81 — 2026-09-02 — bloc 07 — « Garder pour moi » fait redescendre l'histoire à « transcrite ».** Le bloc posait la question sans y répondre. Trois issues étaient possibles ; deux sont mauvaises. Laisser l'histoire en `to_review` la maintiendrait dans la file des relances (règle `recorded_not_validated`, bloc 09) alors que la personne a répondu — on la harcèlerait pour un choix déjà fait. La valider graverait une décision qui doit rester réversible. La transition `KeepStoryPrivate` (`to_review → transcribed`) la sort de la file sans rien graver, et écrit la décision en même temps que l'état pour qu'une chaîne de transcription rejouée ne redemande pas une relecture faite. « Garder pour le livre » reste distinct : coché, il valide et pose `visibility = book_only`. C'est la règle §9 appliquée à la lettre — en cas d'ambiguïté, l'histoire reste privée **et hors livre**. À revalider : oui, c'est un point que le pilote doit observer.

**T-82 — 2026-09-02 — bloc 07 — Une fonctionnalité fermée rend 404, pas 403.** `FeatureClosed` porte un 404. Un « interdit » dirait qu'elle existe et qu'elle est réservée, ce qui invite à insister ; un « introuvable » ne dit rien. Vaut pour le mandat aujourd'hui, et pour tout drapeau fermé ensuite. À revalider : non.

**T-83 — 2026-09-02 — bloc 07 — Un code déjà envoyé ne produit plus une erreur technique.** `OtpService::challenge()` lève `OtpThrottled` au-delà de trois défis par heure, et aucun des trois appelants ne l'attrapait : toucher deux fois « recevoir un code » rendait un 500. Les trois contrôleurs répondent maintenant « un code vous a déjà été envoyé, utilisez celui-là ». Le défaut valait aussi pour la page de code du bloc 03 ; il est corrigé au même endroit. À revalider : non.

**T-84 — 2026-09-02 — bloc 07 — Les tests d'enregistrement tournent en série, avant les autres.** Trois spécifications enregistrent pour de vrai et dépendent du temps réel : `MediaRecorder` doit avoir écrit ses tranches sur le disque du navigateur, et le micro simulé est une ressource partagée. Lancées avec les trente autres, elles échouaient pour une raison qui n'a rien à voir avec le produit. Elles forment un projet Playwright `recorder` à un seul ouvrier, dont le reste dépend — donc en tête, machine au repos. Allonger les délais aurait masqué le problème ; le test de marque avait déjà son projet pour un motif voisin (T-59). À revalider : non.

**T-85 — 2026-09-02 — bloc 07 — Un champ de saisie ne disparaît pas au bout de deux secondes.** La page d'entrée de l'espace affichait le champ du code sous condition de `recentlySuccessful`, qui retombe à faux après deux secondes : le champ s'effaçait sous les yeux de quelqu'un qui met trente secondes à lire son SMS — c'est-à-dire le temps normal, sur un téléphone posé à côté. Un état local le remplace. Corollaire : les deux étapes partagent un **seul** formulaire, parce que recopier la coordonnée du premier dans le second produisait une erreur muette quand la recopie échouait. À revalider : non.

**T-75 — 2026-09-02 — bloc 06 — Le nonce est passé à Inertia, qui crée sa feuille de styles à l'exécution.** Fin de l'enquête ouverte en T-62. La violation résiduelle venait de la barre de progression d'Inertia : elle injecte une balise `<style>` après le rendu, refusée sur `style-src-elem` par la politique stricte des pages narrateur — l'indicateur de chargement ne s'affichait donc pas. Le SDK accepte un nonce (`createInertiaApp({ nonce })`) ; il est lu dans une balise `<meta name="csp-nonce">` alimentée par `Vite::cspNonce()`, la même source que l'en-tête. Diagnostic obtenu en écoutant l'événement `securitypolicyviolation` dans le navigateur, qui donne le fichier et la ligne — ce que la console ne dit pas. Les pages à jeton ne rapportent plus aucune violation. Voie explorée puis abandonnée : `build.cssCodeSplit: false`, qui produit une entrée `style.css` détachée du chunk dans le manifeste, donc plus aucune feuille liée par `@vite`. À revalider : non.

**T-76 — 2026-09-02 — bloc 06 — `E2ELinksSeeder` vide le cache.** Les compteurs de limitation de débit vivent dans Redis et survivent à `migrate:fresh` : le scénario « demander un nouveau lien » — une demande par heure et par jeton, et c'est la bonne règle produit — ne passait donc qu'une fois par heure sur une machine de développement, et échouait aux appels suivants de la suite. Le décor bout en bout comprend ces compteurs ; le seeder, qui refuse de tourner en production, les remet à zéro avec le reste. Le choix inverse — assouplir le limiteur pour les tests — aurait fait tester autre chose que le produit. À revalider : non.

**T-68 — 2026-09-02 — bloc 06 — Le SDK PHP n'expose pas `fallbacks` : on ne l'émule pas.** Vérifié dans `vendor/anthropic-ai/sdk` : le paramètre `fallbacks` existe **uniquement** sur `Beta\Messages\MessageCreateParams`, et pas sur l'appel standard. La règle §9 du bloc s'applique donc telle quelle : aucun repli côté client. Quand `stopReason === 'refusal'`, le verbatim reste seul, le refus est consigné avec `stopDetails->category` et `->explanation` en métadonnée, `TranscriptionReady` est émis quand même, et le support regarde. Le motif est de fond, pas seulement technique : rattraper en silence un refus par un autre modèle, sur un texte destiné à un livre de famille, reviendrait à décider à la place de la famille. À revalider : oui, si le SDK ouvre `fallbacks` sur l'appel standard.

**T-69 — 2026-09-02 — bloc 06 — `pbmedia/laravel-ffmpeg` n'est pas installé.** Le transcodage tient en deux commandes : une lecture `ffprobe` de la durée, un appel `ffmpeg -codec:a libmp3lame`. Le paquet aurait ajouté `php-ffmpeg` et son graphe d'objets pour ça, et surtout un joint de test médiocre — `Process::fake()` éprouve directement les deux commandes, leurs arguments et leurs sorties, y compris le cas qui trompait la chaîne (un `ffmpeg` qui sort en 0 sans écrire de fichier). `ffmpeg` reste requis sur la machine, ses chemins viennent de `FFMPEG_BINARIES` / `FFPROBE_BINARIES`, et `config/laravel-ffmpeg.php` n'existe donc pas. À revalider : non.

**T-70 — 2026-09-02 — bloc 06 — Le renderer dépend d'un port étroit, pas du graphe d'objets du SDK.** Le bloc prévoyait `AnthropicClientFactory` « mockable ». Impossible en l'état : `Anthropic\Services\MessagesService` est déclaré `final`, donc Mockery ne peut pas le doubler, et `Client::$messages` est typé sur cette classe. L'interface `App\Services\Llm\AnthropicMessages` réduit la dépendance à un `create(model, maxTokens, system, messages, outputConfig)` ; `SdkAnthropicMessages` la sert avec le SDK, et le test l'implémente à la main. L'intention du bloc est respectée, et le renderer n'a plus à connaître l'arborescence d'un fournisseur. À revalider : non.

**T-71 — 2026-09-02 — bloc 06 — Les rappels ASR sont signés, et le secret n'a pas de valeur par défaut.** `App\Support\AsrCallback` ajoute `sig = hash_hmac('sha256', recording_id, ASR_CALLBACK_SECRET)` à l'URL de rappel et refuse une signature invalide en comparaison à temps constant. Un webhook non signé serait une porte pour écrire n'importe quel texte dans le récit d'une famille. Corollaire assumé : si `ASR_CALLBACK_SECRET` est vide, la construction de l'URL **lève** au lieu de signer avec une chaîne vide — un secret manquant doit casser bruyamment en déploiement, pas produire une signature devinable. À revalider : non.

**T-72 — 2026-09-02 — bloc 06 — Un courant *par espèce* de transcription, pas un par histoire.** L'index unique partiel porte sur `(story_id, kind) where is_current` : la parole brute, la mise au propre et la correction humaine coexistent en tant que courantes. La conséquence pratique est que corriger un texte ne détrône jamais le verbatim — `EditTranscript` bascule seulement le `edited` précédent — et que `Transcript::readableFor()` choisit dans l'ordre `edited` → `fluide` → `verbatim`. Un unique courant par histoire aurait forcé à effacer la parole de la personne pour afficher une correction. À revalider : non.

**T-73 — 2026-09-02 — bloc 06 — Le transcodage entre dans la chaîne de `CompleteRecording`.** `ReplicateRecording` → `ConcatenateSegments` (s'il y a plusieurs segments) → `TranscodeRecording`, en `Bus::chain`. L'ordre est le fond : on réplique la source avant d'en dériver quoi que ce soit, et c'est le fichier **recollé** qui part au transcodage puis à la transcription — transcrire le premier segment d'un enregistrement interrompu aurait produit un récit tronqué sans que rien ne le signale. Conséquence sur les tests du bloc 04 : `Queue::assertPushed` ne voit que le premier maillon d'une chaîne, les assertions passent à `assertPushedWithChain`, ce qui vérifie en plus l'ordre. À revalider : non.

**T-74 — 2026-09-02 — bloc 06 — Les noms propres suggérés par le modèle ne créent pas d'entrées de lexique.** `FluideResult::properNouns` est rangé dans `metadata.proper_nouns` du rendu fluide, et rien d'autre. Créer automatiquement des `lexicon_entries` reviendrait à laisser un modèle fixer la graphie du nom du village d'une grand-mère. Ce sont des suggestions, qu'un humain valide aux blocs 07, 10 ou 11. À revalider : non.

**T-63 — 2026-09-02 — bloc 05 — Une question avancée par l'Initiateur·rice passe outre la protection de la règle 5.** L'annexe A dit à la fois que l'Initiateur·rice peut réordonner (règle 3) et qu'aucune question de difficulté ≥ 4 ne part avant la sixième histoire validée (règle 5). Les deux se contredisent quand la question avancée est intime. Arbitrage : la règle 3 gagne. La règle 5 protège du **séquencement automatique**, qui ne connaît pas la famille ; elle n'a pas à contredire un choix délibéré de la personne qui l'organise et qui a acheté le service. Le narrateur, lui, garde le droit de ne pas répondre — c'est là que vit sa souveraineté, pas dans le corpus. À revalider : oui, si le pilote montre que des Initiateur·rice·s avancent des questions intimes trop tôt et que des narrateurs décrochent.

**T-64 — 2026-09-02 — bloc 05 — Les dates s'écrivent avec leur décalage.** Complément de T-44. Aligner le fuseau de la session Postgres sur celui de l'application corrigeait l'écriture des dates *dans le fuseau de l'application*, mais laissait un piège : toute date convertie ailleurs — un `->utc()` innocent dans un calcul de planification — se retrouvait décalée de deux heures. Le trait `App\Concerns\StoresDatesWithOffset` fixe `$dateFormat = 'Y-m-d H:i:sP'` sur les modèles du domaine : l'instant est explicite, le fuseau de l'objet PHP devient indifférent. C'est la seule posture sûre pour un produit dont toutes les règles se comptent en heures. À revalider : non.

**T-65 — 2026-09-02 — bloc 05 — Les écouteurs ne sont pas enregistrés à la main.** Laravel découvre ceux de `app/Listeners` d'après le type de leur paramètre. Les enregistrer *en plus* dans `AppServiceProvider` les faisait tourner deux fois : une demande de nouveau lien émettait deux jetons. Le fait que la double révocation de jetons soit idempotente avait masqué le problème depuis le bloc 03. `AppServiceProvider` porte désormais la **liste** des écouteurs actifs en commentaire, sans les enregistrer. À revalider : non.

**T-66 — 2026-09-02 — bloc 05 — Un code à usage unique part sur un seul canal.** Un narrateur peut préférer les deux canaux pour ses questions (`preferred_channel = both`), mais un code part sur un seul : deux codes valides en même temps doubleraient la surface d'attaque pour rien, et la contrainte de `otp_challenges.channel` n'accepte que `sms` ou `email`. `OtpService::channelFor()` résout `both` vers le SMS — celui qu'une personne âgée lit le plus vite — et vers le courriel s'il n'y a pas de téléphone. À revalider : non.

**T-67 — 2026-09-02 — bloc 05 — Les jetons restent masqués dans les journaux, même en local.** Conséquence de T-49 : en local, `LogSmsSender` écrit le corps du SMS dans le journal, mais le lien y ressort en `/r/[redacted]`. Un développeur qui veut suivre un vrai parcours utilise les liens déterministes semés par `E2ELinksSeeder` (`demo-record-linkxxx…`), et non le journal. Le compromis est assumé : un jeton dans un journal est précisément le risque que le registre nomme, et l'affordance de développement existe déjà ailleurs. À revalider : non.

**T-61 — 2026-09-02 — bloc 04 — Le pilote de stockage est lu dans la configuration, jamais déduit de l'environnement.** La liaison de `MediaStorage` interrogeait `runningUnitTests()`. En intégration continue, l'application servie pour le bout en bout tourne avec `APP_ENV=testing` — Laravel la croyait donc en test et lui donnait le stockage en mémoire : les envois partaient vers `https://fake-storage.test`, un hôte qui n'existe pas, et la politique de contenu les refusait. `MEDIA_DRIVER` (`s3` par défaut, `fake` forcé par `phpunit.xml`) rend le choix explicite, comme `SMS_PROVIDER`. Corollaire du même diagnostic : le cookie d'autorisation d'acte sensible ne force plus `secure`, il suit `session.secure` — forcé, il était jeté par le navigateur sur une connexion en clair. À revalider : non.

**T-62 — 2026-09-02 — bloc 04 — `color-scheme` s'exprime en CSS, pas en style en ligne.** `use-appearance` écrivait `document.documentElement.style.colorScheme`. Chrome traite une écriture dans `element.style` comme un style en ligne, que `style-src-attr` ne couvre pas : la politique stricte des pages narrateur la refusait, et les formulaires natifs se lisaient au mauvais schéma. La classe `dark` reste posée par le script ; `color-scheme` la suit depuis `resources/css/app.css`. Une seconde violation de style subsistait, source non identifiée à la clôture du bloc 04 : elle est élucidée et corrigée en T-75. À revalider : non.

**T-54 — 2026-09-02 — bloc 04 — Un envoi multipart par segment, et non un par enregistrement.** Le bloc décrivait une route `parts/{part}/sign` sans numéro de segment, ce qui suppose un seul envoi par enregistrement. Or les conteneurs `webm` et `mp4` ne se concatènent pas octet à octet : déposer deux continuités de flux comme des parts consécutives d'un même envoi produirait un fichier illisible — c'est précisément pourquoi `ConcatenateSegments` existe. Chaque segment a donc son propre envoi multipart, ouvert à la demande par `POST /recordings/{recording}/segments`, et les routes portent le segment. Conséquence : `POST /recordings` n'accepte plus `segments_count` — on ne sait pas d'avance combien d'appels entrants une personne va recevoir. À revalider : non.

**T-55 — 2026-09-02 — bloc 04 — Le brouillon local stocke des octets bruts, pas des `Blob`.** Un test l'a révélé, et le motif est plus grave qu'un détail d'implémentation : Safari iOS invalide les références de `Blob` conservées dans IndexedDB quand il purge un onglet — exactement le scénario auquel ce brouillon doit survivre. Les tranches sont donc écrites en `ArrayBuffer` et les `Blob` reconstruits à la lecture. À revalider : non.

**T-56 — 2026-09-02 — bloc 04 — Deux adresses pour le stockage : une pour le serveur, une pour le navigateur.** En local, MinIO répond sur `http://minio:9000` depuis le conteneur et sur `http://localhost:9001` depuis le Mac. Une URL présignée pour un hôte et présentée à un autre est refusée. `R2_PUBLIC_ENDPOINT` porte l'adresse vue par le navigateur, sert à la présignature et entre dans `connect-src` de la politique de contenu ; en production les deux valeurs coïncident. À revalider : non.

**T-57 — 2026-09-02 — bloc 04 — Les mises en page sont chargées à la demande, et les fournisseurs d'interface quittent l'enveloppe globale.** Le budget de 150 Ko gzip par page narrateur (convention §4) était dépassé de 51 Ko : la barre latérale, les info-bulles et les notifications de l'espace authentifié voyageaient dans le même paquet que la page d'enregistrement. `Toaster` et `TooltipProvider` vivent désormais dans `AppLayout`, et toutes les mises en page passent par `React.lazy`. Effet de bord bienvenu : le style injecté à l'exécution par ces bibliothèques violait la politique de contenu stricte des pages narrateur. Mesure après : sous budget, vérifiée par `tests/e2e/record-budget.spec.ts` qui compresse lui-même les réponses — le serveur local ne le fait pas. À revalider : non.

**T-58 — 2026-09-02 — bloc 04 — MinIO n'implémente pas `PutBucketCors`.** La commande `storage:prepare-local` crée les trois seaux et tente d'appliquer `docker/minio/cors.json` ; MinIO répond `NotImplemented` et autorise toutes les origines par défaut, ce qui convient en local. La règle du fichier est à reporter à la main dans la console Cloudflare au bloc 16, avec l'exposition de l'en-tête `ETag` — sans elle, le navigateur ne peut pas lire la preuve qu'une part est arrivée. À revalider : oui, au bloc 16.

**T-59 — 2026-09-02 — bloc 04 — Chaque scénario bout en bout a son propre lien, et le test de marque tourne seul.** Les tests Playwright tournent en parallèle : un lien d'enregistrement partagé faisait qu'un test enregistrait l'histoire que le suivant attendait vierge. `E2ELinksSeeder` sème un lien par scénario, sur sa propre histoire. Le test de marque, lui, écrit dans des réglages partagés par toute l'application : il vit dans un projet Playwright distinct, exécuté après les autres. L'isolation est dans les données, pas dans un `--workers=1` qui aurait masqué le problème. À revalider : non.

**T-60 — 2026-09-02 — bloc 04 — La route des événements du navigateur ne porte pas `throttle:tokens`.** Ses 20 requêtes par minute et par jeton, faites pour protéger les pages, étouffaient la mesure : une séance agitée produit plusieurs dizaines d'événements. `throttle:client-events` la remplace, avec 120 par minute et par lien plus 240 par minute et par IP. Seule exception au critère de sortie du bloc 03, documentée dans le test qui parcourt la table de routage. À revalider : non.

**T-47 — 2026-09-02 — bloc 03 — Deux politiques de contenu, pas une.** Les pages du produit — publiques, narrateur, proches, espace client — reçoivent une politique stricte : scripts et feuilles de style par `nonce` uniquement, aucun `unsafe-eval`, aucune origine tierce, `object-src 'none'`, `base-uri 'self'`, `form-action 'self'`, `frame-ancestors 'none'`. Le back-office Filament reçoit une politique assouplie (`unsafe-eval` et `unsafe-inline` pour les scripts), parce qu'Alpine évalue ses expressions par `new Function()` : sans cela, le panneau ne fonctionne pas. Le panneau est réservé au personnel et sa double authentification est planifiée au bloc 11. Deux ajustements par rapport à la politique écrite dans le bloc : `style-src-attr 'unsafe-inline'` est nécessaire, React posant des styles en attribut qui ne peuvent pas porter de `nonce` ; et les origines `fonts.googleapis.com` et `fonts.gstatic.com` sont **retirées**, les polices étant auto-hébergées depuis T-40 — autoriser un hôte tiers qu'on n'utilise pas est une faiblesse gratuite. À revalider : oui, au bloc 16, si Filament expose un mode compatible.

**T-48 — 2026-09-02 — bloc 03 — Pas de configuration Telescope : Telescope n'est pas installé.** Le bloc 03 §6.4 demandait d'ignorer les chemins à jeton dans `config/telescope.php`, et le checkpoint d'ouvrir Telescope pour vérifier qu'aucune requête `/r/*` n'y figure. Or T-29 a acté l'absence de Telescope, `laravel/pail` couvrant le besoin en local. La protection reste entière : le masquage vit dans le processeur Monolog, donc dans **tous** les journaux, et non dans la configuration d'un outil. Si Telescope est installé au bloc 11, `ignore_paths` devra reprendre les sept préfixes de `TokenType::urlPrefixes()`. À revalider : oui, au bloc 11.

**T-49 — 2026-09-02 — bloc 03 — Le code à usage unique reste lisible dans le journal, en local seulement.** Le processeur `RedactTokens` masque les jetons partout et dans tous les environnements. Il masque aussi les codes à six chiffres — ce que le bloc ne demandait pas — sauf quand `app()->isLocal()`, parce qu'en local le journal *est* la passerelle d'envoi : `LogSmsSender` y remplace l'opérateur, et masquer le code rendrait impossible de dérouler un parcours narrateur sur sa machine, ce que le checkpoint §7 exige justement. Les jetons, eux, sont masqués même en local : un lien n'a aucune raison de figurer dans un journal. Deux tests fixent les deux comportements. À revalider : non.

**T-50 — 2026-09-02 — bloc 03 — Trois liens de démonstration à valeur connue.** `DemoProjectSeeder` sème un lien d'enregistrement valable, un expiré et un révoqué, de valeurs déterministes (`demo-record-linkxxx…`). Ils permettent d'ouvrir les trois écrans en local sans passer par tinker, et de les éprouver de bout en bout par Playwright. Ils n'existent que dans une base semée par ce seeder, qui refuse de tourner en production, et deux des trois sont morts par construction. Les lignes sont insérées sans `TokenService::issue()`, qui tire un jeton aléatoire par définition — même parti que les états de `StoryFactory` : un seeder construit un décor. À revalider : non.

**T-51 — 2026-09-02 — bloc 03 — Un fournisseur SMS inconnu lève au lieu de se replier.** La liaison de `SmsSender` accepte `fake` et `log`, et lève pour toute autre valeur. Un repli silencieux vers `LogSmsSender` enverrait les codes dans `storage/logs` au lieu de les envoyer aux narrateurs, sans que personne s'en aperçoive. L'arme `twilio` arrive au bloc 05. À revalider : non.

**T-52 — 2026-09-02 — bloc 03 — Une demande de nouveau lien n'émet pas de lien.** Le bouton de la page d'erreur journalise la demande et lève `NewLinkRequested`, que le bloc 05 écoutera pour alerter le support et l'Initiateur·rice. Il n'émet volontairement aucun jeton : un lien qui se régénérerait tout seul depuis une page publique donnerait à quiconque détient l'ancienne URL le pouvoir d'en obtenir une neuve. Plafond : une demande par heure et par lien. À revalider : non.

**T-53 — 2026-09-02 — bloc 03 — Le verrou OTP survit au code, et c'est voulu.** Le code vaut dix minutes, le verrou quinze : après cinq essais ratés, il faut toujours redemander un code, jamais réessayer l'ancien. Le message de la page le dit en clair. Le bloc annonçait « le bon code après déverrouillage émet un `sensitive_grant` » ; c'est un nouveau code qui l'émet. À revalider : non.

**T-41 — 2026-09-02 — bloc 02 — `users.id` reste un identifiant séquentiel.** L'annexe B prévoyait un uuid ; le squelette Laravel a créé la table en `bigint` au bloc 00, et `sessions`, les clés d'accès et le double facteur s'y appuient. `users` n'est jamais exposé par un jeton porteur — l'Initiateur·rice a un compte, le narrateur et les proches n'en ont pas —, donc l'argument d'énumérabilité qui justifie T-23 ne s'y applique pas. Tous les modèles créés au bloc 02 et exposés à l'extérieur (`Project`, `Narrator`, `FamilyMember`, `Question`, `Story`, `Consent`, `Cohort`) sont bien en uuid ordonné. Conséquence : `consents.subject_id` est un `varchar(64)` et non un `uuid`, puisque le sujet peut être un narrateur (uuid) ou un utilisateur (entier). Annexe B corrigée. À revalider : non.

**T-42 — 2026-09-02 — bloc 02 — La visibilité d'une histoire n'appartient pas à l'Initiateur·rice.** `StoryPolicy::manageVisibility()` la refuse au propriétaire du projet comme à l'éditeur désigné, et ne l'accorde qu'au personnel muni de `support.write`, qui n'agit que sur demande écrite. Le narrateur, lui, décidera par jeton (bloc 03) et par OTP pour les actes sensibles ; la personne mandatée arrive au bloc 07. Motif : R-1 dit que le veto du narrateur prime, et il valait mieux inscrire cette souveraineté dans une policy dès maintenant que la reconstituer écran par écran. À revalider : oui, quand le mandat du bloc 07 sera spécifié.

**T-43 — 2026-09-02 — bloc 02 — Une seule classe de transition vers `Validated`, et une seule classe pour chaque retour.** Application de la règle de décision par défaut du bloc 02 §9 : `spatie/laravel-model-states` n'accepte qu'une transition par paire d'états, or `Transcribed → Validated` porte deux gardes (variante A et mandat). `ValidateStory` inspecte donc l'état source et `validated_via`, et lève `ForbiddenTransition`. Corollaire non prévu par le bloc : `UnhideStory` et `RestoreStory` doivent connaître l'état visé pour le comparer à `previous_state` ; elles héritent de `DefaultTransition`, qui le reçoit en paramètre. À revalider : non.

**T-44 — 2026-09-02 — bloc 02 — Le fuseau de la session Postgres suit `APP_TIMEZONE`.** Eloquent sérialise les dates sans décalage (`Y-m-d H:i:s`) et Postgres les interprète dans le fuseau de la session, jusque-là UTC : tout instant écrit depuis l'application, à l'heure de Paris, était enregistré deux heures trop tôt. `config/database.php` passe désormais `'timezone' => env('APP_TIMEZONE')` à la connexion `pgsql`. Le stockage reste en UTC, comme le veut la convention §13 ; seule la conversion en texte est alignée. Test de régression : `tests/Feature/Database/TimestampTimezoneTest.php`. À revalider : non.

**T-45 — 2026-09-02 — bloc 02 — Le retour depuis `archived` attend le bloc 07.** `ArchiveStory` mémorise déjà `previous_state`, mais aucune transition ne sort de `Archived` : le bloc 02 §6.4 n'en prévoit pas, et l'interface des retraits est le sujet du bloc 07. Aucune migration ne sera nécessaire pour l'ouvrir. À revalider : non, le bloc 07 le traite.

**T-46 — 2026-09-02 — bloc 02 — Trois énumérations ajoutées à la liste du bloc.** `ValidationVariant`, `DeletionRequestedBy` et `CohortPhase` ne figuraient pas en §6.1, mais l'annexe B pose une contrainte `check` sur `projects.validation_variant`, `stories.deletion_requested_by` et `cohorts.phase` : sans énumération, ces contraintes auraient été écrites à la main. À revalider : non.

**T-39 — 2026-09-02 — bloc 01 — Les assets publiés par les paquets ne sont ni versionnés ni formatés.** `composer install` exécute `filament:upgrade`, qui republie `public/css/filament`, `public/js/filament` et `public/fonts/filament`. Les versionner créait un conflit permanent avec le formateur en intégration continue. Ils sont désormais ignorés par git et exclus du formatage, comme tout `public/**`. Vérifié : une suppression suivie de `composer install` les régénère. À revalider : non.

**T-40 — 2026-09-02 — bloc 01 — Le panneau d'administration garde la police de Filament.** Lui passer la police de marque aurait fait charger une police depuis Google Fonts à chaque ouverture du back-office. Filament auto-héberge Inter ; le panneau reprend le nom et la couleur principale de la marque, pas sa typographie. Les pages publiques et narrateur, elles, utilisent les polices de marque, auto-hébergées via le greffon de polices de Vite. À revalider : non.

**T-37 — 2026-09-02 — bloc 01 — Toujours la dernière version stable.** Règle demandée par le fondateur et inscrite en `01_CONVENTIONS.md` §6bis : on installe sans contrainte de version, les numéros de la roadmap sont indicatifs, et `composer outdated --direct` comme `npm outdated` doivent être vides à la clôture de chaque bloc. Appliqué immédiatement : Filament 5.7 au lieu du ^4.0 annoncé, puis montée de `typescript` 5.9 → 7.0.2, `@types/node` 22 → 26.4.1, `lucide-react` 0.475 → 1.39, `@laravel/passkeys` 0.2 → 0.4. Porte qualité verte après chaque montée. À revalider : non.

**T-38 — 2026-09-02 — bloc 01 — Caches générés retirés du suivi git.** Le `git add -A` du bloc 00 avait versionné 69 fichiers de `storage/framework/views`, `storage/logs` et `bootstrap/cache`. Ils sont retirés et chaque dossier porte désormais son `.gitignore` (`*` puis `!.gitignore`), comme le prévoit Laravel, pour que l'arborescence survive à un clone sans versionner son contenu. À revalider : non.

**T-111 — 2026-09-03 — bloc 10 — La suite bout en bout se joue en série avant de pousser.** Deux tests d'opt-in partageaient un lien d'invitation. En parallèle — la configuration locale par défaut — ils passaient par chance : le second lisait la page avant que le premier n'ait validé son acceptation. À un seul ouvrier, comme en intégration continue, l'ordre devient déterministe : le premier consomme le lien, et le second trouve « vous avez déjà répondu ». Le produit avait raison, le décor avait tort. Trois conséquences. Un troisième lien est semé (`optin-accept-partial`) — la règle « un lien par scénario » du décor valait déjà pour l'enregistrement (T-59), elle vaut a fortiori pour l'opt-in, qui est **définitif**. La commande canonique de `01_CONVENTIONS.md` §6 passe à `--workers=1`, parce qu'une suite qui passe en parallèle et échoue en série ne prouve rien. Et le diagnostic a coûté une heure pour une raison qui vaut d'être écrite : `php artisan serve` **filtre les variables d'environnement** qu'il transmet au processus serveur (`ServeCommand::$passthroughVariables`), donc une reproduction locale bâtie sur `serve` sert `.env` et non l'environnement exporté. En intégration continue cela passe parce que `$_ENV` y est vide (`variables_order` sans `E`) et que Symfony hérite alors de l'environnement du parent. À revalider : non.

**T-120 — 2026-09-03 — bloc 11 — Les tests du panneau ont soixante secondes, parce qu'une fenêtre TOTP en dure trente.** La suite passait en local et échouait en intégration continue, sans que rien ne distingue les deux environnements — même configuration d'application reproduite, même ordre, même sérialisation. La cause est une **course** : `freshTotp()` attend la fenêtre suivante quand la courante a déjà servi, et cette attente peut consommer trente secondes — soit exactement le délai par défaut d'un test Playwright. En local les deux connexions tombaient de part et d'autre d'une frontière de fenêtre ; en CI, plus rapide, elles tombaient du même côté. Le projet `admin` accorde donc soixante secondes. La leçon vaut d'être notée : « passe en local, échoue en CI » n'est pas toujours une différence d'environnement, c'est parfois une course que le local perd rarement. À revalider : non.

**T-123 — 2026-09-03 — bloc 12 — Une photo est du contenu : l'Initiateur·rice ne voit que ses propres dépôts avant le partage.** L'invariant du bloc 08 — rien n'est visible avant que le narrateur ait partagé — ne nommait que le texte et la voix. Une photo en est aussi : sur une histoire non partagée, le tableau de bord ne montre donc à l'Initiateur·rice que ce qu'**elle** a déposé, ni les photos du narrateur ni celles d'un proche. Il aurait été facile de le perdre : le tableau de bord est « son » espace, et rien n'y rappelle qu'une photo jointe par quelqu'un d'autre à un récit non partagé ne lui appartient pas encore. Le filtre vit dans `PhotoPresenter::forInitiator`, une seule porte, avec deux tests. À revalider : non.

**T-124 — 2026-09-03 — bloc 12 — Les textes des photos vivent dans `common`.** Rangés dans `lang/fr/narrator.php`, ils s'affichaient en **clé brute** sur la page famille et le tableau de bord : `Translations::forSpace` n'envoie au front que `common` plus le fichier de l'espace courant, et c'est délibéré — les pages narrateur s'ouvrent en 4G sur de vieux téléphones. Le dépôt de photo est le premier composant partagé par les quatre espaces, et sa place est donc `common`. Découvert par un test bout en bout qui cherchait « Ajouter une photo » et lisait `common.photos.add`. À revalider : non.

**T-125 — 2026-09-03 — bloc 13 — Les critères R-6 sont mesurés sur les données qui existent, et le critère de pages est le vrai verrou.** Deux points. Le premier : le bloc décrivait `sensitive_reviewed` comme « toutes les histoires avec `sensitive_flags` ont `visibility` posée explicitement », mais il n'existe ni colonne `sensitive_flags` sur `stories` ni horodatage de décision de visibilité. Les marqueurs existent bel et bien — dans les **métadonnées du rendu Fluide**, écrits par le bloc 06 — et le signal de décision explicite est `share_decision` valant `share` ou `keep_private` ; `decide_later` ne compte pas, c'est le contraire d'une décision. Ajouter une colonne que rien n'aurait peuplée aurait produit un critère toujours satisfait, donc une garde de façade. Le second point est arithmétique et vaut d'être écrit : à 280 mots par page, les 12 000 mots de R-6 font 48 pages, bien en dessous des 60 exigées. **Le critère de pages est donc le verrou réel** — il faut environ 15 400 mots pour un livre de dix chapitres, ou des photos, qui pèsent une demi-page chacune. Conséquence pour la jauge de l'espace : afficher un pourcentage de mots ferait croire à une famille qu'elle y est presque alors qu'il lui manque un quart de la matière. Un test le nomme explicitement. À revalider : **oui** pour la jauge — c'est un choix d'affichage qui touche à l'attente d'une famille.

**T-126 — 2026-09-03 — bloc 06 — `fluide-v2` : la négation orale est conservée, et un drapeau sensible se définit par qui il expose.** La lecture humaine du corpus (`docs/corpus/`) a trouvé deux défauts qu'aucun test ne pouvait voir, puisqu'aucun test n'appelle le vrai modèle. Le premier : `fluide-v1` rétablissait la négation complète environ une fois sur trois — « je sais pas » devenait « je ne sais pas », tandis que « je voulais pas » et « il disait rien » passaient intacts. C'était un écart au prompt lui-même, qui promet de conserver « son niveau de langue » ; sauf que « niveau de langue » est trop abstrait pour être appliqué, et l'inconstance est pire que l'erreur — personne ne peut raisonner sur une règle qui vaut une fois sur trois. `v2` la nomme, avec ses exemples. Le second : `essai-05`, le souvenir d'un grand-père taciturne revenu de la première guerre, ressortait avec le drapeau `conflict`, qui désigne une brouille entre personnes de la famille. Or un drapeau sensible non arbitré **bloque le livre** (R-6, `ComputeBookReadiness`) : une fausse alerte coûte une étape de friction **et** annonce à une famille un conflit qui n'existe pas, et quelques fausses alertes suffisent à ce que plus personne ne lise les vraies. `v2` définit chaque drapeau par la personne qu'il pourrait exposer, nomme les identifiants du schéma en anglais plutôt qu'en français — le modèle n'a plus à traduire pour remplir l'énumération — et ajoute la contre-règle : « un récit de vie parle de mort, de guerre, de deuil et de misère : ce n'est pas sensible en soi ». Les cinq textes relus avec `v2` : les deux défauts corrigés, aucune régression, un seul effet de bord — `essai-04` conserve désormais le « Ben… » d'ouverture que `v1` retirait comme tic. On le garde : un mot de trop se supprime à la relecture familiale, un détail inventé ne se rattrape pas. Conséquence de forme : `PROMPT_VERSION` passe à `fluide-v2` et **`fluide-v1.txt` reste sur le disque**, parce que les récits déjà rendus portent dans leurs métadonnées l'étiquette du prompt qui les a produits — supprimer le fichier ferait mentir la métadonnée. À revalider : non.

**T-127 — 2026-09-03 — bloc 04 — « Recommencer » n'a jamais rien fait, et la case était cochée.** Trouvé par un humain devant la page, pendant le checkpoint du bloc 07. `AlreadyRecorded.tsx` recevait `onRestart?: () => void` et son bouton appelait `onRestart?.()` — sauf que la page est rendue par le serveur (`inertia('narrator/AlreadyRecorded', …)`) et **qu'une fonction ne traverse pas Inertia**. La prop était donc toujours indéfinie, l'appel toujours sans effet : aucun message, aucune erreur, aucun changement à l'écran. La page était exactement le cul-de-sac que son propre commentaire jurait qu'elle n'était pas. Deux enseignements dépassent le défaut lui-même. **Le premier :** rien ne pouvait l'attraper. PHPStan ne voit pas les props React ; TypeScript trouvait la prop optionnelle donc valide ; aucun test de composant n'existait pour cette page ; et un bouton mort ne casse rien qu'une assertion regarde. Un test de composant a été écrit d'abord, qui vérifie que le clic **appelle le serveur** — pas qu'il appelle une fonction. **Le second, plus grave :** la capacité n'existait pas non plus côté serveur. La seule transition vers « enregistrée » partait de « proposée », donc même le bouton rebranché, la chaîne aurait échoué à la confirmation de l'envoi. La case §6 du bloc 04 était cochée sur une intention. La correction ajoute la transition `RestartRecording` — `[recorded, transcribed, to_review] → proposed`, le seul retour complet de la machine — une action, une route `POST /r/{token}/restart` sans code (même geste de regret que le masquage, et lui demander un SMS la ferait renoncer), et un journal d'audit `restarted Story` : « pourquoi la famille entend-elle un autre enregistrement qu'à l'origine ? » est une question à laquelle il faut pouvoir répondre des mois plus tard. Trois choix de fond dans cette transition. On revient jusqu'à « proposée » plutôt que d'ouvrir un second chemin vers « enregistrée » : du point de vue de la personne la question est de nouveau posée, et tout ce qui suit reprend le chemin déjà éprouvé. L'enregistrement précédent n'est pas touché — `InitiateRecording` le fera passer `is_current` à faux quand le suivant s'ouvrira, donc fermer la page juste après avoir cliqué ne coûte rien. Et la décision de partage est **effacée** : elle portait sur un enregistrement qui ne sera plus le courant, et la garder ferait partager sans redemander. Enfin le geste est refusé à partir de « validée » : des proches ont pu l'entendre, et remplacer l'audio derrière un lien qu'ils gardent leur ferait écouter autre chose que ce qu'on leur avait annoncé — ce geste-là s'appelle retrait, et il passe par l'espace narrateur. À revalider : non. **Correction dans la foulée, signalée par le même humain dix minutes plus tard :** la première version de la garde refusait `proposed`, si bien qu'un **second** clic — bouton retour, double tap, réseau lent — rendait 403, alors que l'action était déjà écrite pour ne rien faire dans ce cas. La garde contredisait l'idempotence de ce qu'elle protégeait. « Il n'y a rien à faire » et « c'est interdit » sont deux réponses différentes, et la seconde au visage de quelqu'un de 82 ans qui voulait simplement recommencer est un défaut à part entière. `mayRestart()` inclut donc `proposed` : le geste est permis, il ne fait rien, la personne arrive sur la page qu'elle attendait, et l'audit n'enregistre qu'une ligne. Deux tests le tiennent, dont un qui compte les lignes d'audit.

**T-128 — 2026-09-03 — bloc 04 — Le décor local n'avait consenti à rien, et toute la chaîne s'arrêtait au mot à mot.** Trouvé pendant le checkpoint du bloc 07, en jouant « décider plus tard » pour de vrai. `RenderFluide` refuse de rendre un texte sans le consentement `ai_rendering`, et c'est la bonne règle. Mais le semis créait ses narratrices par `AddNarrator` seul, sans passer par l'opt-in qui accorde les cinq consentements : la base de démonstration comptait **zéro** ligne de consentement. Toute chaîne réellement jouée en local s'arrêtait donc au verbatim, **en silence** — un simple `fluide.skipped_no_consent` en niveau `info` dans le journal. La page de relecture affichait « Alors euh je me souviens… » au lieu du texte mis au propre, et la conclusion évidente pour un humain devant l'écran était « le rendu IA est cassé ». Deux raisons de consigner cela plutôt que de le corriger discrètement. **La première :** les scénarios du bloc 07 portent des transcriptions **semées**, verbatim et fluide, ce qui masquait entièrement le trou — le décor paraissait complet et ne l'était que pour les histoires qu'on n'enregistrait pas. Un décor qui ne représente aucun état atteignable en production ne prouve rien, et celui-là décrivait une narratrice qui reçoit des questions sans avoir jamais rien accepté. **La seconde :** le saut est journalisé en `info`, pas en `warning`. C'est défendable en production, où le cas est impossible par construction — les cinq consentements sont posés ensemble à l'acceptation, donc une personne qui reçoit des questions les a forcément. La garde est une ceinture, pas un chemin réel, et il n'y a donc pas de trou produit derrière ce défaut. Correction : un `consentingNarrator()` dans le semis, qui accorde les cinq consentements **par `RecordConsent`** — un décor qui insérerait ses lignes à la main ne prouverait pas que le chemin réel fonctionne. Les six narratrices de projets actifs y passent ; celle du scénario d'invitation garde son absence de consentement, qui est précisément ce que l'opt-in doit produire. Un test tient l'invariant : toute narratrice d'un projet actif du décor porte les cinq consentements. À revalider : non.

**T-129 — 2026-09-03 — bloc 04 — Aucune navigation côté client ne fonctionnait en mode développement, et l'intégration continue ne pouvait pas le voir.** Signalé en test humain : « je vois bien l'histoire sur le lien famille, mais rien ne se passe quand je clique dessus ; si je clique et que je recharge, je suis bien sur le détail ». Symptôme déroutant, et signature nette dans la console — `You are calling ReactDOMClient.createRoot() on a container that has already been passed to createRoot() before`. **Deux racines React sur `#app` :** l'une tenait le routeur et mettait l'URL à jour, l'autre tenait le DOM et gardait l'écran. La cause a demandé de remonter jusqu'à l'initiateur des requêtes : `app.tsx` était chargé **deux fois**, la seconde avec le marqueur d'invalidation de Vite (`app.tsx?t=1788455407732`), et l'initiateur de cette seconde requête était **`app.tsx` lui-même**. Le module se réimporte donc, son corps tourne une seconde fois, `createInertiaApp` appelle `createRoot` sur un conteneur déjà monté. L'horodatage étant constant d'un chargement à l'autre, il ne s'agissait pas d'un rechargement à chaud en cours mais d'une invalidation **restée en place**, portée par un serveur de développement en vie depuis cinq heures et des dizaines de modifications PHP. La garde vit dans `app.tsx`, le seul module qui monte quelque chose : un drapeau sur `window`, et le second passage ne fait rien. Elle ne coûte rien en production, où le module s'exécute une seule fois. **Ce qui mérite d'être retenu dépasse le correctif : l'intégration continue ne peut structurellement pas attraper cette classe de défaut.** Elle exécute `npm run build` puis Playwright, donc elle éprouve les assets **construits** et ne lance jamais le serveur de développement. Vérifié pendant le diagnostic : en retirant `public/hot`, le même clic navigue correctement. Les tests bout en bout cliquent pourtant bien ce lien et passent — ils passaient en testant l'autre moitié du monde. Aucun test automatique n'a de prise sur le mode que l'équipe utilise toute la journée, et c'est un humain devant un navigateur qui l'a trouvé. **Défaut voisin corrigé au passage :** la politique de sécurité ne déclarait pas l'origine du serveur Vite, donc `connect-src` refusait `ws://localhost:5176` et le websocket de rechargement à chaud ne se connectait **jamais** en local. Ce n'était pas la cause du double montage — le correctif ne l'a pas fait disparaître — mais c'était un vrai défaut, silencieux lui aussi : aucun rechargement à chaud depuis le début du projet. `SecurityHeaders` ajoute désormais l'origine HTTP du serveur aux hôtes de médias et son websocket au seul `connect-src`, sous deux gardes — le fichier `hot` doit exister **et** l'application ne pas être en production, pour qu'un `hot` oublié dans une image livrée n'ouvre aucune origine. Un `ws://` dans `img-src` serait du bruit, et le bruit est ce qui cache les erreurs dans un en-tête de sécurité. À revalider : non.

**T-130 — 2026-09-03 — bloc 08 — « 1 personne(s) ont écouté » : deux fautes en huit mots, dans un message dont le seul rôle est de donner envie de raconter la suite.** Trouvé en préparant le checkpoint du bloc 08 : le résumé des réactions partait en SMS chez la narratrice avec « Hier, 1 personne(s) ont écouté vos histoires. » La parenthèse est un pluriel qu'on n'a pas écrit, et l'accord du verbe est faux au singulier. Laravel sait choisir entre deux formes depuis toujours (`trans_choice`) ; s'en passer fait payer au lecteur le confort de celui qui écrit, et ce lecteur-là a 82 ans et reçoit le message sur son téléphone. Corrigé par une chaîne à deux formes — « Hier, une personne a écouté vos histoires. » / « Hier, :count personnes ont écouté vos histoires. ». Deux gardes accompagnent le correctif, parce que le défaut était d'une classe et non un accident. La première refuse tout pluriel entre parenthèses (`(s)`, `(e)`, `(es)`, `(x)`) dans un texte visible : un seul coupable existait dans tous les fichiers de langue, et il n'en existera plus. La seconde étend le scanner de clés d'`I18nKeysTest` à `trans_choice` en plus de `__` — sans quoi passer une clé au pluriel la faisait sortir du champ de vérification, et une clé que personne ne vérifie finit par manquer. Noté au passage, et non corrigé : la forme longue `{1} … |[2,*] …` a été écartée parce qu'elle contient une astérisque, ce que refuse le contrôle de cohérence de `ForbiddenVocabularyTest` — lequel a raison, puisque cette astérisque est précisément ce qu'il cherche pour détecter qu'on lit du PHP brut au lieu de valeurs traduites. La forme à deux formes est de toute façon plus lisible. À revalider : non.

**T-131 — 2026-09-03 — bloc 08 — L'audio du décor n'était pas un MP3 lisible, et ma feuille de test induisait en erreur.** Signalé en test humain : « le compteur de l'audio reste à zéro, je pense que le problème vient de là. » Le diagnostic était bon. L'élément `<audio>` rendait `MEDIA_ERR_SRC_NOT_SUPPORTED` (`error: 4`, `readyState: 0`, `duration: null`) : les trames semées annonçaient 128 kbit/s dans leur en-tête — ce qui impose des trames de 417 octets — mais n'en faisaient que 404, et le fichier entier durait une seconde là où la base annonçait 124. Aucun décodeur ne peut lire cela. Conséquence en chaîne : pas de lecture, donc pas de progression, donc aucun seuil de trente secondes franchi et **zéro écoute enregistrée** — la moitié du bloc 08 était injouable, et personne ne l'avait vu parce que les tests bout en bout n'écoutent jamais. Le semis écrit désormais des trames conformes : MPEG-1 Layer III, 32 kbit/s, 44,1 kHz, mono, sans CRC, dont la taille se déduit de l'en-tête (`floor(144 × 32000 / 44100)` = 104 octets) et la durée du nombre d'échantillons d'une trame, 1152. Quarante-cinq secondes pèsent 180 Ko, assez pour franchir le seuil sans alourdir le semis, et la durée annoncée en base est celle du fichier — un lecteur qui affiche deux minutes sur quarante-cinq secondes mentirait à qui écoute. Fabriqué en PHP et non par `ffmpeg` : le conteneur en a un, l'intégration continue non, et un décor qui ne se sème que sur la machine du développeur n'est pas un décor. Vérifié dans un vrai navigateur — 44,8 s de durée, `readyState: 4`, cinq rapports de progression, seuil des trente secondes franchi. **Deuxième moitié de l'écart, et elle est de moi :** le point 3 semblait échoué alors qu'il fonctionne. Deux raisons, aucune attribuable au produit. La notification est **différée de soixante secondes** à dessein — un cœur et un merci envoyés d'affilée ne font qu'un seul SMS — et ma consigne ne le disait pas, si bien qu'on conclut trop tôt. Et le point 4 de ma feuille, qui bascule le drapeau des réactions, était jouable **avant** le point 3 : une fois le projet passé à « lendemain matin », plus rien ne part tout de suite et rien à l'écran ne le dit. La feuille affiche désormais l'état du drapeau, dit que l'ordre compte, nomme le délai d'une minute, et se termine par la remise à l'état initial. Une commande `demo:reaction-timing` bascule le drapeau et antidate une réaction, au lieu d'une ligne de `tinker` de deux cents caractères où la moindre coquille est muette ; l'envoi, lui, n'est pas enveloppé, parce que `reactions:send-digests` est la vraie commande et c'est elle que la vérification doit exercer. À revalider : non.

**T-132 — 2026-09-04 — design — Les fondations de la direction artistique : palette, Fraunces variable, thème clair seul.** Première étape de la propagation de la maquette validée le 3 septembre (`docs/design/`). Quatre choses, chacune avec sa raison. **La palette** passe par une migration de réglages et non par `config/brand.php` seul : la base est la source de vérité depuis le bloc 01, et la configuration n'est qu'un repli pour un semis neuf — sans migration, tout environnement déjà migré aurait gardé le vert acide de Remento. Le sens des champs ne change pas, leur valeur si : `accent` reste la couleur d'action, mais l'action passe d'un vert acide qui se fondait dans une marque verte à une terracotta qui est la seule couleur chaude saturée de la page. Six tokens de dessin fixes s'ajoutent en CSS (lin, sable, or, sauge, forêt profond, terracotta foncée) : ce sont des choix de dessin et non d'identité, donc pas des réglages d'administration — si la marque change, le lin et l'or restent. **Fraunces est auto-hébergée en police variable**, à la main, depuis `public/fonts` : le greffon de polices sert des graisses fixes, et la direction artistique repose précisément sur les axes SOFT et WONK — c'est ce qui fait un serif de livre plutôt que de site. Deux fichiers (romain, italique, sous-ensemble latin, OFL) tirés de Google Fonts, 264 Ko à eux deux, déclarés par `@font-face`. Vérifié dans un vrai navigateur : `document.fonts.check` répond vrai, et `font-variation-settings` vaut « SOFT 50, WONK 1 » sur un titre. Deux familles au lieu de quatre : Instrument Sans et Newsreader, que le kit avait laissées, ne servaient à rien et pesaient sur une page narrateur en 4G. **Le mode sombre est retiré de bout en bout** — middleware `HandleAppearance`, cookie, route et page de réglage, onglets, crochet, script de détection dans la vue racine, bloc `.dark` du CSS. Décision du fondateur : un livre se lit sur du papier. Les utilitaires `dark:` des composants du kit restent dans les sources, inertes puisque rien ne pose la classe ; ils partiront avec chaque famille de pages. **Une bascule sémantique globale et mécanique** : les dix-sept panneaux de mise en avant qui portaient la couleur d'accent passent sur lin — sinon ils seraient devenus terracotta, et un panneau de la couleur du bouton fait croire à un bouton — et les quatre-vingt-sept filets à 40 % de la couleur atténuée passent sur sable. Les boutons d'action, eux, restent en couleur de marque jusqu'à ce que chaque famille de pages soit reprise : un bouton forêt en attendant est lisible, un panneau terracotta ne l'est pas. Un test a bougé : celui qui vérifiait le nonce sur le script du mode sombre vérifie désormais qu'**aucun** script exécutable ne part sans nonce, en ligne ou chargé par Vite — plus fort que la version qu'il remplace. À revalider : non.

**T-133 — 2026-09-04 — design — La direction artistique est propagée aux quatre familles de pages, par trois règles mécaniques relues à l'écran.** Suite de T-132. Vingt-cinq pages, quatre familles, quatre commits : narrateur, famille, public, Initiateur·rice. **Les règles**, appliquées par script puis vérifiées sur des captures à 390 px : l'action de la page passe en terracotta, et elle seule — un bouton se voit par son isolement ; ce qui n'est pas l'action de la page reste en couleur de marque quand c'est une commande (l'onglet actif, le bouton de lecture audio, le dépôt de photo) ou prend un contour dans la couleur de marque quand c'est un secondaire ; les panneaux de mise en avant sont sur lin, les cartes blanches sur le crème de la page, les filets sable. **Ce que la relecture à l'écran a corrigé après le script :** les cartes de choix de la relecture avaient hérité d'une graisse qui rendait leur description injonctive ; sur la page d'essai, « Offrir à un proche » n'est pas l'action de la page ; sur la page d'écoute, la réaction l'est, et ses deux boutons sont deux formes d'un seul geste. Un script applique une règle ; il ne sait pas ce qu'une page veut faire faire. **La page d'accueil** est la maquette validée, portée dans l'ordre du dossier 01 §4 et non dans le sien — la maquette validait une direction, le dossier fixe un raisonnement —, avec les sept engagements dans leur formulation canonique (la maquette les avait résumés en trois) et la preuve du corpus posée à côté d'eux. Le titre reste la promesse du dossier ; le prix affiché est celui des réglages, pas celui de l'analyse (89 € contre 49 €). Axe y a trouvé deux défauts de structure — une liste d'options qui n'avait pas à être un `dl`, deux exemples illustrés qui étaient des `div` étiquetées et sont des figures — et le test d'ordre des sections lit désormais les identifiants des titres plutôt que leur libellé, l'ordre n'ayant pas bougé. **Deux corrections hors dessin, trouvées en chemin :** la connexion menait à `/dashboard`, la page gabarit du kit aux rectangles hachurés — le nom de route reste, il redirige vers l'espace, qui est le vrai tableau de bord, et la destination de Fortify suit ; le pictogramme du kit de démarrage tenait encore lieu de logo sur les pages de connexion et dans l'en-tête authentifié — c'est la marque, lue dans les réglages. Les tokens du kit (`--primary`, `--card`, `--border`…) sont exprimés dans la palette, ce qui habille les pages d'authentification et de réglages sans toucher aux composants. **Ce que la passe laisse ouvert, et le dit :** les pages d'authentification du kit sont en anglais et en dur — elles n'ont pas d'espace de langue (`Translations::SPACES` ne connaît pas `auth.`) ; c'est un chantier d'internationalisation, pas de dessin, et c'est la première chose que voit une personne qui se connecte. Les utilitaires `dark:` restent dans les composants du kit, inertes. À revalider : non.

**T-134 — 2026-09-04 — design — La page d'accueil suit la structure de Remento, section par section, avec nos mots et nos faits.** Décision du fondateur, le soir du 4 septembre : « ce sont les leaders, alors copions-les ». **Ce qui est copié :** l'ordre des sections ; le bandeau d'offre en une ligne au-dessus de la barre (« Une année de questions + le livre relié : tout compris, 89 € »), qui convertit chez eux ; la répétition de l'appel à l'action à chaque palier de la lecture, avec le secondaire « Comment ça marche ↓ » dans le héros ; la fiche produit livre-plus-téléphone ; « ce que comprend votre achat » avec le prix ; la bande de confiance ; la garantie ; « pensé pour les grands-parents » ; la double page ; la relecture ; le cadeau ; les options ; les questions. **Ce qui ne l'est pas, parce que ce n'est pas vrai chez nous :** la presse, les avis, les vidéos de clients, les célébrités. Les emplacements ne sont pas construits vides ; ils viendront avec les familles pilotes. **Ce qui change de fond :** le prix passe à 89 € (l'analyse du fondateur le disait, le réglage pilote restait à 49) par une migration de réglages, la configuration servant de repli ; le dossier stratégique (`docs/dossier`) dit encore 49 €, il est antérieur et n'est pas réécrit. La phrase de marque devient « Le livre de leurs souvenirs, avec leur voix à chaque page. » ; « qui va réellement au bout » n'était pas fluide. La garantie affichée est « satisfait ou remboursé pendant trente jours », sans justification à fournir, au lieu des quatorze jours de rétractation : c'est un engagement commercial qui dépasse la loi, il est ajouté à la relecture juridique (05 §6, ligne 12quinquies). La ligne sous le bouton dit « Un seul paiement sécurisé, pas d'abonnement. Ses souvenirs restent privés. » « Notre histoire » est écrite à partir de l'histoire du fondateur, à la première personne, sans le nommer. **Deux règles d'écriture s'ajoutent :** aucun tiret long dans un texte visible (« ça fait trop IA ») ; les neuf qui restaient dans les fichiers de langue sont remplacés par une virgule, un point ou deux points ; et les appels à l'action restent à la première personne, à la place et au rythme de Remento. **Ce que la garde de vocabulaire a rattrapé pendant la passe :** quatre « pour toujours » copiés de leur « forever », interdits par R-11 parce que personne ne peut le promettre ; remplacés par ce qui est vrai : « un accès qui ne s'arrête pas avec l'année », « il n'y a rien à renouveler ». L'ordre des sections du dossier 01 §4, que T-133 avait fait primer sur la maquette, cède à celui du leader ; le test bout en bout lit les identifiants des titres dans le nouvel ordre. La capture de la relecture est une vraie capture de notre page (`public/img/landing/relecture.png`), pas une maquette. À revalider : oui, le texte de la garantie de trente jours, par le conseil, avant l'ouverture des ventes.

**T-118 — 2026-09-03 — bloc 12 — Le contrôle antivirus est écrit à la main, pas emprunté.** `sunspikes/clamav-validator` était prévu par le bloc ; sa dernière version stable ne déclare pas Laravel 13, et son travail tient en quarante lignes de protocole `INSTREAM`. `ClamavScanner` le remplace, derrière un port `Scanner` avec un `FakeScanner` — ce qu'une règle de validation de paquet n'aurait pas permis, et dont on ne peut pas se passer : ClamAV parle un protocole de socket que rien dans Laravel n'intercepte, et le démon n'existe pas en intégration continue. Deux choix consignés avec lui. `INSTREAM` plutôt que `SCAN <chemin>`, parce que le démon tourne dans son propre conteneur et ne voit pas le disque de l'application. Et **en cas de panne du démon, on refuse** : laisser passer un fichier non scanné parce que l'antivirus est tombé revient à ne pas avoir d'antivirus, et cela arriverait le jour où il compte. À revalider : non.

**T-119 — 2026-09-03 — bloc 12 — La conversion HEIC n'est pas éprouvée de bout en bout, et le checkpoint s'en charge.** L'ImageMagick de l'image Sail sait **lire** le HEIC — c'est ce qui compte pour le produit — mais pas l'écrire ; ni ffmpeg ni libheif-examples n'y sont. Fabriquer une fixture demanderait de committer un binaire dans le dépôt. Trois tests couvrent ce qui est couvrable : la **capacité** de décodage, qui criera le jour où une image système la perd ; le fait que la sortie est toujours du JPEG, éprouvé sur PNG et WEBP, qui suivent le même chemin ; et le message de refus, qui est la décision produit (règle §9 du bloc). Le point 1 du checkpoint l'éprouve sur une vraie photo d'iPhone, ce qui est de toute façon la seule preuve qui vaille. À revalider : non.

**T-121 — 2026-09-03 — bloc 12 — Trois pièges du paquet de médiathèque, tous découverts à l'exécution.** Sa migration publiée emploie `morphs('model')` — des clés entières — alors que tous les modèles de domaine ont des UUID : la première photo jointe échouait sur un « invalid input syntax for type bigint ». Les méthodes de dimension d'une conversion (`width()`) délèguent au pilote d'image et n'en rendent plus la conversion, donc `performOnCollections()` et `nonQueued()` doivent **précéder** la taille, sinon ils s'appliquent au mauvais objet. Et sa configuration publiée référence `Spatie\MediaLibraryPro\Models\TemporaryUpload`, une classe de la version payante absente : nommée par sa chaîne, elle ne fait plus échouer l'analyse statique. La migration a par ailleurs été reprise aux conventions du projet — `jsonb`, `timestampsTz`, et un index sur `(model_type, model_id, collection_name)` que le paquet ne prévoit pas alors que c'est **la** requête de cette table. À revalider : non.

**T-122 — 2026-09-03 — bloc 12 — Une photo retirée est supprimée, pas mise à la corbeille.** Une histoire retirée passe par une corbeille et reste récupérable trente jours ; une photo non. La différence a une raison : une histoire est un récit qu'on peut regretter d'avoir retiré, une photo qu'on retire est presque toujours une photo qu'on n'aurait pas dû déposer. Garder trente jours ce qu'un proche vient de retirer serait garder exactement ce qu'il a voulu faire disparaître. À revalider : non.

**T-112 — 2026-09-03 — bloc 11 — L'empreinte du journal se calcule sur des formes canoniques, jamais sur le texte inséré.** Première version : le hachage prenait `occurred_at` et `payload` tels qu'on les avait écrits. Toute ligne relue était alors incohérente avec elle-même, et `audit:verify` criait à la falsification sur un journal intact. Deux causes, la seconde plus sournoise : Postgres rend l'horodatage dans son propre format textuel, et le type `jsonb` **réordonne les clés** et normalise les espaces. L'horodatage est donc normalisé en UTC à la seconde — pas à la microseconde, dont la précision varie avec le pilote — et le contenu est réencodé clés triées, les listes gardant leur ordre. À revalider : non.

**T-113 — 2026-09-03 — bloc 11 — Le contexte de l'acteur se déduit d'une route résolue, pas de `runningInConsole()`.** Sous PHPUnit, `App::runningInConsole()` rend vrai **même pendant qu'une requête de test est traitée** : toute lecture depuis le panneau s'inscrivait comme venant de la console, et le test qui ouvrait réellement une page l'a montré. La présence d'une route résolue (`Route::current()`) est la vraie question — une commande, une file et le planificateur n'en ont pas. Même règle pour la capture de l'adresse IP, qui n'a de sens que dans une requête. À revalider : non.

**T-114 — 2026-09-03 — bloc 11 — La double authentification obligatoire casse tout décor de test qui l'ignore, et c'est voulu.** Rendre la MFA obligatoire a fait échouer neuf tests écrits aux blocs 01 et 10 : un compte du personnel sans second facteur ne peut plus ouvrir une page. Les décors portent désormais `withAppAuthentication()`, et le compte semé en production n'en a **pas** — sa première connexion passe par la configuration, comme pour un nouveau membre de l'équipe. La suite bout en bout franchit le second écran en **calculant** son code TOTP contre un secret semé par `E2ELinksSeeder`, qui refuse de tourner en production : désactiver la garde pour les tests aurait produit une garde qu'on ne teste pas. Le secret vit sur les colonnes de Fortify, un seul magasin — deux finiraient par diverger, et le jour où ils divergent quelqu'un reste dehors avec un code valide en main. À revalider : non.

**T-115 — 2026-09-03 — bloc 11 — Filament refuse la réémission d'un même code TOTP, et deux tests s'y sont cassés.** La protection contre le rejeu est juste ; sa conséquence est que deux connexions dans la même fenêtre de trente secondes échouent, et que l'échec ressemble à un mot de passe faux. Le décor a désormais un `freshTotp()` qui se souvient de la fenêtre déjà consommée et attend la suivante. Deux corollaires notés dans la configuration : les tests du panneau tournent **seuls et en série** (la page de connexion limite aussi les tentatives), et une seule connexion sert tout un parcours. À revalider : non.

**T-116 — 2026-09-03 — bloc 11 — Deux pièges de Filament, découverts à l'exécution.** Le premier : un défaut de propriété ne peut pas appeler `__()`, donc `$navigationGroup` devient une méthode — l'erreur est un `Constant expression contains invalid operations` au chargement de la classe, sans rapport apparent avec la traduction. Le second, plus vicieux : `canEdit()` existe déjà sur `Resource` en public, et une méthode privée du même nom fait échouer le chargement de la classe. Les vingt-deux méthodes `can*` du framework sont donc des noms réservés ; les helpers d'autorisation maison portent d'autres noms (`canWrite`, `canEditText`, `canRefund`, `canReissue`). À revalider : non.

**T-117 — 2026-09-03 — bloc 11 — Le titre des pages de liste est écrit, pas dérivé.** Filament dérive le titre du libellé de la ressource en le passant par une mise en capitales à l'anglaise : « Les histoires » devenait « Les Histoires ». En français, seul le premier mot prend la majuscule. Le trait `TitlesInFrench` rend le libellé tel quel. Ce n'est pas un détail de goût : les mots qu'une équipe lit toute la journée finissent par décider comment elle écrit aux familles. À revalider : non.

**T-104 — 2026-09-03 — bloc 10 — Une seule migration de Cashier est conservée.** `cashier:install` publie trois migrations : les colonnes client sur `users`, les abonnements et leurs articles. Le pilote ne vend qu'un paiement unique — aucun abonnement, aucun essai, aucun renouvellement. Les deux tables d'abonnement sont donc retirées et remplacées par une migration maison, `add_stripe_customer_columns`, qui n'ajoute que ce qui sert : `stripe_id`, `pm_type`, `pm_last_four`, `trial_ends_at`. Motif : deux tables vides invitent à croire qu'un abonnement existe, et la prochaine personne qui lira le schéma cherchera le code qui les remplit. Si un abonnement apparaît un jour, on republiera les migrations de Cashier telles quelles. À revalider : non.

**T-105 — 2026-09-03 — bloc 10 — Le paiement passe par un port, comme l'ASR et le LLM.** Le SDK de Stripe n'utilise pas le client HTTP de Laravel : il a son propre transport (cURL), que `Http::preventStrayRequests()` ne voit pas. Un test qui aurait oublié un doublon serait donc parti sur le réseau, avec de vraies clés si elles traînaient dans `.env` — et aurait pu créer une session de paiement réelle. `CheckoutSessions` est une interface étroite (une méthode), avec `StripeCheckoutSessions` et `FakeCheckoutSessions` derrière, choisies par `STRIPE_DRIVER` et jamais par l'environnement (même règle que T-61). Le faux garde ce qu'on lui a demandé : c'est ainsi qu'on vérifie que le tunnel envoie les bons identifiants de prix sans appeler Stripe. À revalider : non.

**T-106 — 2026-09-03 — bloc 10 — Une clé de langue définie deux fois disparaît en silence.** En ajoutant les textes de l'espace Initiateur·rice, j'ai écrit un second `'settings' => [...]` dans `lang/fr/initiator.php`. PHP garde la dernière définition et jette la première : quatre messages d'action (`settings.saved`, `settings.lexicon_added`, …) ont disparu sans qu'aucun test n'échoue, parce qu'aucun test ne les appelait. Rien dans la chaîne — ni Pint, ni PHPStan, ni Pest — ne signale un doublon dans un tableau littéral. `I18nKeysTest` porte désormais deux gardes de plus : un détecteur de clés redéfinies qui lit les **jetons** du fichier (le tableau rendu par `require` est déjà dédoublonné, il ne peut rien révéler), et la vérification que toute clé appelée par `__()` dans `app/`, `database/` et `routes/` existe. Cette seconde garde a immédiatement trouvé un vrai trou : `notifications.transcription_failed.*`, appelé depuis le bloc 06, n'avait jamais eu de texte. À revalider : non.

**T-107 — 2026-09-03 — bloc 10 — Le rendu serveur est éteint par défaut.** Inertia livre `ssr.enabled = true` dans sa configuration publiée. Or le rendu serveur est un **service séparé** (`php artisan inertia:start-ssr`) : activé par défaut, chaque requête de test tente une connexion vers `127.0.0.1:13714`, ce que la suite s'interdit (aucune dépendance réseau, convention §5). Il s'allume par `INERTIA_SSR_ENABLED`, et `phpunit.xml` le force à `false`. Deux conséquences de mise en œuvre : `ssr.tsx` importe ses mises en page **statiquement** — `renderToString` ne sait pas attendre un composant paresseux, et un `lazy()` y rendrait une page vide — et il écrit son `resolve` à la main, parce que la signature de rendu serveur l'exige alors que le greffon `@inertiajs/vite` n'en injecte un que là où il manque. À revalider : non.

**T-108 — 2026-09-03 — bloc 10 — Les variantes `printed-card` et `audio-message` du cadeau ne sont pas livrées.** Le bloc les décrit ; le drapeau `gift-experience` rend `ecard` pour tout le monde, et c'est son défaut assumé (§6.1). La carte A6 imprimable demande la chaîne PDF du bloc 13 (Browsershot, Chromium, poppler-utils, aucun encore installé) ; le message vocal de l'acheteur demande un enregistreur hors page narrateur **et** un point d'envoi ouvert à un visiteur non connecté, puisque l'étape 3 précède la création du compte. Les deux valeurs restent dans le drapeau, dans l'énumération et dans la validation, parce que le modèle de données doit être prêt à les mesurer. Les livrer maintenant serait construire deux chemins que personne ne peut atteindre, dont un qui ouvre une surface d'envoi anonyme. À reprendre au bloc 13 pour la carte. À revalider : **oui** — c'est un choix de périmètre.

**T-109 — 2026-09-03 — bloc 10 — La contrainte de joignabilité empêchait l'effacement obligatoire des coordonnées.** `narrators_reachable_check` exigeait un courriel **ou** un téléphone. La contrainte est juste — sans coordonnée, aucun lien ne part — mais elle rendait impossible l'obligation du doc 04 §2 : effacer les coordonnées d'une personne qui n'a jamais dit oui, trente jours après la dernière relance. La commande `narrators:delete-unaccepted-contacts` échouait sur une violation de contrainte, et c'est le test écrit pour elle qui l'a découvert — la commande existait depuis le début du bloc sans avoir jamais tourné. Même forme de correction qu'au bloc 07 pour l'audio d'origine (T-80) : on ne retire pas la garde, on nomme l'exception. `contact_deleted_at` la nomme, et sa **date** l'accompagne — « quand ces coordonnées ont-elles été effacées ? » est exactement ce qu'une demande RGPD demande, et un enregistrement de journal ne survit pas à une rotation. À revalider : non.

**T-110 — 2026-09-03 — bloc 10 — Playwright tourne depuis le Mac, pas depuis le conteneur.** Les trois tests qui enregistrent pour de vrai échouaient sur un délai dépassé quand la suite était lancée par `sail npx playwright test`. La cause n'était pas le temps : les URLs présignées d'envoi sont signées pour `R2_PUBLIC_ENDPOINT`, l'adresse **vue par le navigateur**, soit `http://localhost:9001` — le port que Docker publie sur l'hôte. Un navigateur lancé dans le conteneur y trouve `ERR_CONNECTION_REFUSED`. La commande canonique de `01_CONVENTIONS.md` §6 est corrigée, avec la raison écrite à côté : allonger les délais aurait masqué le problème pendant des semaines. Effet de bord découvert au passage : les tests d'accessibilité analysaient la page **avant** que React l'ait rendue, et ne trouvaient donc rien à reprocher à un DOM vide. Ils attendent maintenant le titre de niveau 1 avant d'analyser. À revalider : non.

**T-29 — 2026-09-02 — bloc 00 — Versions réelles du kit de démarrage.** `laravel new --react --pest --database=pgsql` installe Laravel 13, Inertia 3, React 19 avec le React Compiler, Vite 8, Tailwind 4, Pest 5, PHP ^8.3 (8.5.7 dans le conteneur Sail), Fortify avec clés d'accès et double facteur. Larastan, Pint, Sail, Telescope absent mais Pail présent. La roadmap tablait sur des versions antérieures ; on suit le kit. À revalider : non.

**T-30 — 2026-09-02 — bloc 00 — Vite+ (`vp`) remplace ESLint, Prettier et l'appel direct à Vitest.** Le kit livre `vite-plus`, une chaîne unifiée qui fournit `oxlint` (lint), `oxfmt` (format), Vitest (tests) et Vite (build). `npm run check` exécute format, lint et types ; `vp test` exécute Vitest avec toutes ses options. Conséquence : ne pas installer `eslint`, `prettier` ni `vitest` séparément, contrairement à ce qu'annonçait le bloc 00. La configuration de lint et de format vit dans `vite.config.ts`, pas dans des fichiers dédiés. À revalider : non.

**T-31 — 2026-09-02 — bloc 00 — On adopte les noms de scripts du kit.** `composer lint` (corrige), `composer lint:check`, `composer types:check` (PHPStan), `composer test` (enchaîne lint:check, types:check et les tests), `composer ci:check` (ajoute le front). Un alias `composer check` est ajouté vers `ci:check`. La roadmap proposait `analyse` et `check` ; on garde la convention Laravel pour éviter deux vocabulaires. À revalider : non.

**T-32 — 2026-09-02 — bloc 00 — Wayfinder pour les routes typées côté React.** Le kit génère des représentations TypeScript des routes et actions Laravel dans `resources/js/routes` et `resources/js/actions` (ignorés par git). Les composants référencent les routes par ces helpers plutôt que par des chaînes. À revalider : non.

**T-33 — 2026-09-02 — bloc 00 — L'image Sail est utilisée telle quelle.** Elle contient déjà ffmpeg, imagick, les dépendances Playwright, pgsql, redis, intl, bcmath, gd. `sail:publish` a donc été annulé ; seul `docker/pgsql/create-testing-database.sql` est conservé, car `compose.yaml` le monte. On republiera l'image aux blocs 12 et 13, pour ClamAV et poppler-utils. À revalider : non.

**T-34 — 2026-09-02 — bloc 00 — Ports locaux décalés.** Ce projet utilise 8001 (app), 5176 (Vite), 54323 (Postgres), 6381 (Redis), 1027 et 8027 (Mailpit), 9001 et 8901 (MinIO). `APP_URL=http://localhost:8001`. Motif d'origine : la pile `fenomn-laravel` occupait les ports par défaut. Elle est arrêtée depuis, mais ses conteneurs existent toujours et seront relancés ; on garde donc le décalage plutôt que de créer un conflit à chaque bascule de projet. À revalider : non.

**T-36 — 2026-09-02 — bloc 00 — Le dossier de travail est `/Users/serra/Codes/racines`.** Renommé depuis `remento-clone` après la clôture du bloc 00. Conséquence : le nom de projet Docker Compose, dérivé du nom du dossier, passe de `remento-clone` à `racines`. Les anciens conteneurs et volumes ont été supprimés après vérification qu'ils ne contenaient aucune donnée (zéro utilisateur, cinq migrations du kit, aucun bucket), puis la pile a été remontée et migrée sous le nouveau nom. Le doublon `remento-clonde` a disparu. À revalider : non.

**T-35 — 2026-09-02 — bloc 00 — Incident disque Docker.** La machine virtuelle Docker (58,4 Go) était pleine à 99 %, ce qui faisait échouer la vérification des signatures apt sur tous les dépôts pendant la construction de l'image. Résolu en supprimant le cache de construction (22,86 Go annoncés) et les images orphelines (10,89 Go réellement libérés). La limite de disque de Docker Desktop, alors réglée à 61 035 Mio pour 292 Go libres sur le Mac, a été relevée depuis. À revalider : non.
